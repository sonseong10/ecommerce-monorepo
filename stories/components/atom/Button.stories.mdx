import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import Button from "/components/Button.tsx";
import styled from "styled-components";

<Meta
  title="Components/Atom/Button"
  component={Button}
  argTypes={{
    text: {
      description: "버튼 내부 텍스트",
      type: { summary: "string" },
    },
    btnType: {
      description: "버튼 유형",
      type: { summary: "string" },
      table: {
        defaultValue: {
          summary: "normal",
        },
      },
      options: ["normal", "border", "ghost"],
      control: { type: "select" },
    },
    btnSize: {
      description: "버튼 사이즈",
      table: {
        defaultValue: {
          summary: "normal",
        },
      },
      type: { summary: "string" },
      options: ["lg", "md", "normal", "sm", "xsm", "xs"],
      control: { type: "select" },
    },
    thin: {
      description: "버튼 테두리 굵기",
      type: { summary: "boolean" },
      table: {
        defaultValue: {
          summary: false,
        },
      },
    },
    iconPosition: {
      description: "버튼 내부아이콘 위치",
      type: { summary: "string" },
      options: ["before", "center", "after"],
      control: { type: "select" },
    },
    iconName: {
      description: "버튼 내부아이콘 설정",
      type: { summary: "string" },
      options: [],
      control: { type: "select" },
    },
    color: {
      description: "버튼 색상",
      type: { summary: "string" },
      options: [
        "primary",
        "dark",
        "negative",
        "warning",
        "positive",
        "white",
        "blue",
        "green",
        "gray",
        "lightGray",
        "cyan",
      ],
      control: { type: "select" },
      table: {
        defaultValue: {
          summary: "dark",
        },
      },
    },
  }}
/>

# Button

`<button></button>`요소이며 어드민에서 공통된 디자인 버튼 사용을 위해 컴포넌트 제작되었습니다.

## 최근변경 이력

| 구분 | 기간       | 설명              |
| ---- | ---------- | ----------------- |
| 1    | 2023-02-18 | 스토리북 이관작업 |

## 사용법

```tsx
import Button from "../components/Button";

function MyComponent() {
  return <Button text="Button" />;
}
```

export const Template = args => <Button {...args} />;

export const Title = styled.strong`
  font-weight: 500;
  font-size: 16px;
  margin-right: -100px;
`;

### 기본

<Canvas>
  <Story
    name="Default"
    args={{
      text: "버튼입니다",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Default-Disable"
    args={{
      text: "버튼입니다",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### 버튼타입

<Canvas columns={2}>
  <Title>normal</Title>
  <Story
    name="normalType"
    args={{
      text: "버튼입니다",
    }}
  >
    {Template.bind({})}
  </Story>
  <Title>border</Title>
  <Story
    name="borderType"
    args={{
      text: "버튼입니다",
      btnType: "border",
    }}
  >
    {Template.bind({})}
  </Story>
  <Title>ghost</Title>
  <Story
    name="gohstType"
    args={{
      text: "버튼입니다",
      btnType: "ghost",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### 버튼사이즈

<Canvas>
  <strong>xs</strong>
  <Story
    name="xs"
    args={{
      text: "버튼입니다",
      btnSize: "xs",
    }}
  >
    {Template.bind({})}
  </Story>
  <strong>xsm</strong>
  <Story
    name="xsm"
    args={{
      text: "버튼입니다",
      btnSize: "xsm",
    }}
  >
    {Template.bind({})}
  </Story>
  <strong>sm</strong>
  <Story
    name="sm"
    args={{
      text: "버튼입니다",
      btnSize: "sm",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## 사용해보기

<Canvas withSource="open">
  <Story
    name="Button"
    args={{
      text: "버튼입니다",
      btnType: "border",
      btnSize: "normal",
      thin: true,
      iconPosition: "before",
      iconName: "Plus",
      color: "green",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button" />
